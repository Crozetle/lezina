# Файл настроек приложения
# Этот файл читается классом AppConfig (Singleton) один раз при старте программы

app.name=MyApp
app.version=1.0.0
app.author=Leonov

database.host=localhost
database.port=5432
database.name=postgres

server.port=8080
server.timeout=30
